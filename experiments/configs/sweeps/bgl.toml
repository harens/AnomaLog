name = "bgl"
dataset = "bgl_entity_chronological"
model = "template_frequency_default"
description = "BGL baseline matrix across chronological split models and train fraction."

[[axes]]
path = "sweep.model"
values = ["template_frequency_default", "naive_bayes_default"]

[[axes]]
path = "dataset.sequence.train_fraction"
values = [0.01, 0.1, 0.2]
