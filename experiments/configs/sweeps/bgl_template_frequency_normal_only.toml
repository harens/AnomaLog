name = "bgl_template_frequency_normal_only"
dataset = "bgl_entity_normal_only"
model = "template_frequency_default"
description = "BGL template-frequency baseline at 1% and 10% normal-only train fractions."

[[axes]]
path = "dataset.sequence.train_fraction"
values = [0.01, 0.1]
