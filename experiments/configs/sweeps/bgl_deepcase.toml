name = "bgl_deepcase"
dataset = "bgl_entity_chronological"
model = "deepcase_default"
description = "BGL DeepCASE sweep over the fixed chronological train fraction."

[[axes]]
path = "dataset.sequence.train_fraction"
values = [0.01, 0.1, 0.2]
