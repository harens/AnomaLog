name = "bgl_entity_supervised"
dataset_name = "BGL"
preset = "bgl"
template_parser = "drain3"
description = "Built-in BGL preset grouped by entity with both labels eligible for the training split."

[sequence]
grouping = "entity"
train_fraction = 0.8
train_on_normal_entities_only = false
