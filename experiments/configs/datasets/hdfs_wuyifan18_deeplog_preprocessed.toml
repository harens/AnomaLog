name = "hdfs_wuyifan18_deeplog_preprocessed"
description = "DeepLog and DeepCASE HDFS reproduction on the wuyifan18 preprocessed session files with the exact hdfs_train / hdfs_test file boundary, plus a template-frequency sanity baseline."

[dataset]
name = "hdfs_wuyifan18_preprocessed_exact_boundary"
dataset_name = "HDFS_WUYIFAN18_DEEPLOG_PREPROCESSED"
preset = "hdfs_wuyifan18_deeplog_preprocessed"
template_parser = "identity"
description = "DeepLog and DeepCASE HDFS reproduction config using the wuyifan18 preprocessed session files and the exact hdfs_train / hdfs_test file boundary, plus a template-frequency sanity baseline."

[dataset.sequence]
grouping = "entity"

[dataset.cache_paths]
data_root = "data/hdfs_wuyifan18_deeplog_preprocessed"
cache_root = ".cache/hdfs_wuyifan18_deeplog_preprocessed"

[[models]]
ref = "deeplog_default"
run_group = "deeplog"

[models.overrides]
"model.parameter_detection_enabled" = false

[[models]]
ref = "template_frequency_default"
run_group = "baselines"

# [[models]]
# ref = "deepcase"
