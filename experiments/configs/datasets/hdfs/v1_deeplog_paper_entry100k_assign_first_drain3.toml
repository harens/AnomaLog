extends = "v1_deeplog_paper_entry100k_assign_first.toml"
name = "hdfs_v1_deeplog_paper_entry100k_assign_first_drain3"
description = "DeepLog HDFS paper-reproduction ablation with Drain3 template mining across the key-only and full variants, plus template-frequency and Markov sanity baselines on the same split."

[dataset]
name = "hdfs_v1_deeplog_paper_entry100k_assign_first_drain3"
description = "DeepLog HDFS paper-reproduction ablation: first 100k raw entries with assign-by-first-event straddler handling, Drain3 template mining, and normal-only entity training, shared by the key-only and full variants."
template_parser = "drain3"
