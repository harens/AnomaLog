name = "naive_bayes_default"
detector = "naive_bayes"
smoothing = 1.0
phrase_ngram_min = 1
phrase_ngram_max = 2
top_k_phrases = 5
anomalous_posterior_threshold = 0.5
description = "Supervised sequence-level Naive Bayes sanity baseline over template phrase n-grams."
