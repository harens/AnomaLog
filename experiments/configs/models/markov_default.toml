name = "markov_default"
detector = "markov"
primary_metric_scope = "event_level_detection"
secondary_metric_scopes = ["sequence_level_detection"]
description = "Transition baseline that can report event-level detection on datasets with per-event labels."
