name = "deeplog_default"
detector = "deeplog"
description = "Normal-only DeepLog reproduction config: training filters to normal targets, while labels remain available for evaluation and eligibility bookkeeping."
